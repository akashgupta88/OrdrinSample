== Ruby On Rails Sample Application

This is a sample application on Rails which uses the Ordrin API to fetch a list of restaurants which deliver to a certain address.

It also fetches the menu for a particular restaurant.
